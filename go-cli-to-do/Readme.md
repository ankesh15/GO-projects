# Go CLI-To-Do

A simple **command-line To-Do application** built with **Golang**.  
You can **add**, **list**, and **mark tasks as done**, with all data stored locally in a `tasks.json` file.

---

## 🚀 Features
- Add new tasks  
- List all tasks  
- Mark tasks as completed  
- Data stored persistently in JSON  

---
